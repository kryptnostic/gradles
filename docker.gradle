if ( !ext.has('rhizome') ){
  ext.rhizome=""
}

docker {
    name "kryptnostic/${projectName}:latest"
    dependsOn tasks.distTar
    files 'src/main/resources/rhizome.yaml', 'src/main/resources/rhizome.yaml.prod'
    buildArgs([IMG_VER: "${version}", IMAGE_NAME: "${projectName}", ENV: "${rhizome}"])
}
